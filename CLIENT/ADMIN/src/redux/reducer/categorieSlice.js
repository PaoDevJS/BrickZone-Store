import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    categories: []

}

export const categoriesSlice = createSlice({
    name: 'Categories',
    initialState,
    reducers: {
        
    }
})